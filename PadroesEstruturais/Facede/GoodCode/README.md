## GoodCode

O padrão facede basicamente encapsula toda lógica de um processo
dentro de uma só classe, isso é ruim por inumeros motivos, mas 
pelo monos possui a ventagem de apresentar grande complexidade 
quando chamamos um método.